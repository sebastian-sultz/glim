// Layout.jsx

import Navbar from './Navbar';
import Background from './Background';

const Layout = () => {
  return (
    <div className="layout">
      <Background/>
    <Navbar/>
    </div>
  );
};

export default Layout;
